function "public"."fc_lista_dfe(bigint)" {
  text = """

select id, mensagem, chave, datahora
from tbl_dfe 
where id = pid_dfe
"""
  returnType = TABLE(id_dfe bigint, mensagem character varying, chave character varying, datahora timestamp without time zone)
  arguments = <
    {
      name = pid_dfe
      type = bigint
      mode = IN
    }

  >
  language = sql
  owner = systaxdfe
}

